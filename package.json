{
  "name": "uno-web",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build:engine": "pnpm --filter @uno/engine build",
    "test:engine": "pnpm --filter @uno/engine test",
    "build:api": "pnpm build:engine && pnpm --filter @uno/api build",
    "dev:api": "pnpm build:engine && pnpm --filter @uno/api dev",
    "dev:web": "pnpm --filter @uno/web dev",
    "build:web": "pnpm build:engine && pnpm --filter @uno/web build"
  }
}
